apiVersion: v1
kind: Service
metadata:
  name: opensearch-dashboards-nodeport
  namespace: logging
spec:
  type: NodePort
  selector:
    app: opensearch-dashboards
  ports:
    - port: 5601
      targetPort: 5601
      nodePort: 30601
